name: Release UPM Package

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Resolve package version
        id: package
        shell: bash
        run: |
          PACKAGE_JSON="Packages/com.masachuang.solidtext3d/package.json"
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          if [[ -z "$VERSION" || "$VERSION" == "null" ]]; then
            echo "package.json version is missing" >&2
            exit 1
          fi

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          else
            TAG_NAME="v${VERSION}"
          fi

          if [[ "$TAG_NAME" != "v${VERSION}" ]]; then
            echo "Tag $TAG_NAME does not match package version v${VERSION}" >&2
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag_name=$TAG_NAME" >> "$GITHUB_OUTPUT"

      - name: Create package archive
        shell: bash
        run: |
          VERSION="${{ steps.package.outputs.version }}"
          STAGE_DIR="dist/com.masachuang.solidtext3d"
          ARCHIVE_PATH="dist/com.masachuang.solidtext3d-${VERSION}.zip"

          mkdir -p "$STAGE_DIR"
          rsync -a \
            --exclude '.meta' \
            --exclude 'Tests/' \
            Packages/com.masachuang.solidtext3d/ "$STAGE_DIR/"

          cd dist
          zip -r "com.masachuang.solidtext3d-${VERSION}.zip" "com.masachuang.solidtext3d"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG_NAME="${{ steps.package.outputs.tag_name }}"
          VERSION="${{ steps.package.outputs.version }}"
          ARCHIVE_PATH="dist/com.masachuang.solidtext3d-${VERSION}.zip"

          if gh release view "$TAG_NAME" > /dev/null 2>&1; then
            gh release upload "$TAG_NAME" "$ARCHIVE_PATH" --clobber
            exit 0
          fi

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            gh release create "$TAG_NAME" "$ARCHIVE_PATH" --generate-notes
          else
            gh release create "$TAG_NAME" "$ARCHIVE_PATH" --target "${GITHUB_SHA}" --generate-notes
          fi
